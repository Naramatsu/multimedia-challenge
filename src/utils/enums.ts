export enum RequestStatus {
  NOT_LOADED = "NOT_LOADED",
  LOADING = "LOADING",
  ERROR = "ERROR",
  LOADED = "LOADED",
}

export enum ToastTypes {
  ERROR = "ERROR",
  SUCCESS = "SUCCESS",
  EMPTY = "",
}
